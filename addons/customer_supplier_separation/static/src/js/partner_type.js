/** @odoo-module **/

import { registry } from "@web/core/registry";

registry.category("services").add("customer_supplier_separation_banner", {
    start() {
        // Placeholder service for future UI enhancements / unlicensed banner hooks.
    },
});
